/* Scheduler APIs

   The commands sent by the driver to inform scheduler of the events such as:
   queue creation, destruction, suspension, or any changes to its priority.
   Each API is described later under APIs section

*/
